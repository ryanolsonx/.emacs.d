# -*- mode: snippet -*-
# name: React Redux Create File Connected Class Component
# key: rcccf
# --
import React from 'react';
import PropTypes from 'prop-types';
import {bindActionCreators} from 'redux';
import {connect} from 'react-redux';

class ${1:`(ro-get-file-name)`} extends React.Component {
  static propTypes = {
    $2
  };

  render() {
    $0
    return (
      <div>Hello $1</div>
    );
  }
}

function mapStateToProps(state) {
  return {
    // TODO: map state here.
  }
}

function mapDispatchToProps(dispatch) {
  return bindActionCreators({
    // TODO: bind actions here.
  }, dispatch);
}

export default connect(
  mapStateToProps,
  mapDispatchToProps
)($1);